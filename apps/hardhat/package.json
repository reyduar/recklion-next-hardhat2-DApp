{
  "name": "hardhat",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc -b",
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:local": "hardhat test --network localhost",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:ganache": "hardhat run scripts/deploy.ts --network ganache",
    "deploy:amoy": "hardhat compile && hardhat run scripts/deploy.ts --network polygonAmoy && npm run copy-abis",
    "deploy:sepolia": "hardhat compile && hardhat run scripts/deploy.ts --network sepoliaInfura && npm run copy-abis",
    "copy-abis": "node scripts/copy-abis.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.2",
    "@types/node": "^24.10.0",
    "dotenv": "^17.2.3",
    "ethers": "^6.15.0",
    "hardhat": "^2.27.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
